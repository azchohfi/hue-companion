<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="HueWindows.Controls.RoomCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HueWindows.Controls"
    xmlns:vm="using:HueWindows.Core.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    PointerEntered="CardRoot_PointerEntered"
    PointerExited="CardRoot_PointerExited">

    <Grid x:Name="OuterContainer">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="ActiveStates">
                <!-- No transitions - border opacity animated manually in code-behind -->
                <!-- Icon foreground is set in code-behind via UpdateIconColor() to match room's light colors -->
                <VisualState x:Name="Inactive"/>
                <VisualState x:Name="Active"/>
            </VisualStateGroup>

            <!-- HoverStates handled in code-behind for theme-aware colors -->
            <VisualStateGroup x:Name="HoverStates">
                <VisualState x:Name="Default"/>
                <VisualState x:Name="Hover"/>
            </VisualStateGroup>

            <VisualStateGroup x:Name="DragStates">
                <VisualState x:Name="Normal"/>
                <VisualState x:Name="Dragging"/>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <!-- Main card container -->
        <Grid x:Name="CardRoot"
              CornerRadius="16"
              MinHeight="120"
              ManipulationMode="TranslateY"
              ManipulationStarted="CardRoot_ManipulationStarted"
              ManipulationDelta="CardRoot_ManipulationDelta"
              ManipulationCompleted="CardRoot_ManipulationCompleted"
              Tapped="CardRoot_Tapped">
            <!-- Background set in code-behind for theme-aware gradients -->
            <Grid.Shadow>
                <ThemeShadow BlurRadius="20" Opacity="0.3"/>
            </Grid.Shadow>

            <Grid x:Name="LightingEffects" CornerRadius="16" IsHitTestVisible="False">
                <Border CornerRadius="16" Margin="1" Opacity="0.7">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="{ThemeResource CardInnerHighlightColor}" Offset="0"/>
                            <GradientStop Color="#00FFFFFF" Offset="0.55"/>
                            <GradientStop Color="{ThemeResource CardInnerShadowColor}" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>
                <Border CornerRadius="16" Margin="1" Opacity="0.6">
                    <Border.Background>
                        <RadialGradientBrush Center="0,0" RadiusX="1" RadiusY="1">
                            <GradientStop Color="{ThemeResource CardCornerGlowColor}" Offset="0"/>
                            <GradientStop Color="#00FFFFFF" Offset="0.65"/>
                        </RadialGradientBrush>
                    </Border.Background>
                </Border>
                <Border CornerRadius="16" Margin="1" Opacity="0.55">
                    <Border.Background>
                        <RadialGradientBrush Center="1,1" RadiusX="1" RadiusY="1">
                            <GradientStop Color="{ThemeResource CardCornerShadowColor}" Offset="0"/>
                            <GradientStop Color="#00FFFFFF" Offset="0.7"/>
                        </RadialGradientBrush>
                    </Border.Background>
                </Border>
                <Border CornerRadius="16" Margin="1" BorderThickness="1" Opacity="0.55">
                    <Border.BorderBrush>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="{ThemeResource CardEdgeGlowColor}" Offset="0"/>
                            <GradientStop Color="#00FFFFFF" Offset="0.45"/>
                            <GradientStop Color="{ThemeResource CardInnerShadowColor}" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.BorderBrush>
                </Border>
            </Grid>

            <!-- Solid Outline Border - Moved here for valid XAML structure (after properties) -->
            <Border x:Name="OutlineBorder"
                    Canvas.ZIndex="0"
                    Grid.RowSpan="3"
                    CornerRadius="16"
                    BorderThickness="2"
                    IsHitTestVisible="False"
                    Opacity="0"/>

            <!-- Padded Content Wrapper -->
            <!-- ZIndex 1 ensures it's above the border -->
            <Grid Margin="16" Canvas.ZIndex="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header: Icon and Name -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Icon container with circular background -->
                    <Border x:Name="IconContainer"
                            Grid.Column="0"
                            Width="40" Height="40"
                            CornerRadius="20"
                            Background="{ThemeResource CardIconBackgroundBrush}">
                        <Grid>
                            <Border x:Name="IconGlow"
                                    CornerRadius="20"
                                    Background="Transparent"
                                    Opacity="0.7"/>
                            <FontIcon x:Name="RoomIcon"
                                      Glyph="{Binding RoomIcon}"
                                      FontSize="18"
                                      Foreground="{ThemeResource CardIconForegroundBrush}"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="RoomNameText"
                                   Text="{Binding RoomName}"
                                   FontWeight="SemiBold"
                                   FontSize="14"
                                   Foreground="{ThemeResource CardPrimaryTextBrush}"
                                   TextTrimming="CharacterEllipsis"/>
                        <TextBlock x:Name="LightCountText"
                                   FontSize="12"
                                   Foreground="{ThemeResource CardSecondaryTextBrush}"
                                   Margin="0,2,0,0">
                            <Run Text="{Binding LightCount}"/>
                            <Run Text=" lights"/>
                        </TextBlock>
                    </StackPanel>
                </Grid>

                <!-- Middle spacer for drag gesture -->
                <Grid Grid.Row="1"/>

                <!-- Bottom: Toggle and Brightness -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Toggle switch -->
                    <ToggleSwitch x:Name="RoomToggle"
                                  Grid.Column="0"
                                  Style="{StaticResource GradientToggleSwitchStyle}"
                                  IsOn="{Binding IsOn, Mode=TwoWay}"
                                  Tapped="RoomToggle_Tapped"
                                  VerticalAlignment="Center"/>

                    <!-- Brightness bar -->
                    <Grid Grid.Column="1"
                          VerticalAlignment="Center"
                          Margin="8,0,12,0"
                          Height="4">
                        <!-- Track background -->
                        <Border Background="{ThemeResource CardTrackBackgroundBrush}"
                                CornerRadius="2"/>
                        <!-- Fill indicator -->
                        <Border x:Name="BrightnessFill"
                                Background="{ThemeResource CardTrackFillBrush}"
                                CornerRadius="2"
                                HorizontalAlignment="Left"/>
                    </Grid>

                    <!-- Brightness percentage -->
                    <TextBlock x:Name="BrightnessText"
                               Grid.Column="2"
                               FontSize="12"
                               Foreground="{ThemeResource CardTertiaryTextBrush}"
                               VerticalAlignment="Center"
                               MinWidth="36"
                               TextAlignment="Right">
                        <Run Text="{Binding BrightnessPercent}"/>
                        <Run Text=" %"/>
                    </TextBlock>
                </Grid>
            </Grid>
            

        </Grid>


    </Grid>
</UserControl>
