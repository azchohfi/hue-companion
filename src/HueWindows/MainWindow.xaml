<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="HueWindows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HueWindows"
    xmlns:views="using:HueWindows.Views"
    Title="Hue Windows">

    <Grid>
        <NavigationView
            x:Name="NavView"
            IsBackButtonVisible="Auto"
            IsSettingsVisible="True"
            ItemInvoked="NavView_ItemInvoked"
            BackRequested="NavView_BackRequested"
            PaneDisplayMode="LeftCompact"
            IsPaneToggleButtonVisible="True"
            OpenPaneLength="240"
            ExpandedModeThresholdWidth="1000"
            CompactModeThresholdWidth="600"
            IsTabStop="False"
            IsTitleBarAutoPaddingEnabled="False">

            <NavigationView.PaneHeader>
                <Grid Height="40" Margin="12,0,0,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="12">
                        <FontIcon Glyph="&#xE781;" FontSize="20" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                        <TextBlock Text="Hue" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </NavigationView.PaneHeader>

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Home" Tag="Dashboard">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE80F;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" Padding="0"/>
        </NavigationView>
    </Grid>
</Window>
