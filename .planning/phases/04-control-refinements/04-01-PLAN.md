---
phase: 04-control-refinements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/HueWindows/Views/SceneBuilderPage.xaml.cs
autonomous: true

must_haves:
  truths:
    - "Brightness slider only sends API update after user stops dragging for 150ms"
    - "Color picker only sends API update after user stops adjusting for 150ms"
    - "UI updates (text, timeline render) still happen immediately for responsiveness"
  artifacts:
    - path: "src/HueWindows/Views/SceneBuilderPage.xaml.cs"
      provides: "Debounced slider and color picker handlers"
      contains: "_brightnessDebounceTimer"
  key_links:
    - from: "BrightnessSlider_ValueChanged"
      to: "ViewModel.UpdateLightForKeyframeAsync"
      via: "DispatcherTimer (150ms delay)"
      pattern: "_brightnessDebounceTimer.*Tick"
    - from: "KeyframeColorPicker_ColorChanged"
      to: "ViewModel.UpdateLightForKeyframeAsync"
      via: "DispatcherTimer (150ms delay)"
      pattern: "_colorDebounceTimer.*Tick"
---

<objective>
Add 150ms debouncing to brightness slider and color picker to prevent API flooding during drag operations.

Purpose: Prevent hundreds of API calls when user drags brightness slider or adjusts color picker, while maintaining immediate visual feedback in the UI.
Output: SceneBuilderPage.xaml.cs with debounced handlers matching the proven pattern from LightDetailPage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-control-refinements/04-RESEARCH.md

# Reference implementation (proven debounce pattern)
@src/HueWindows/Views/LightDetailPage.xaml.cs (lines 350-419)

# Target file
@src/HueWindows/Views/SceneBuilderPage.xaml.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add debouncing to brightness and color picker handlers</name>
  <files>src/HueWindows/Views/SceneBuilderPage.xaml.cs</files>
  <action>
Add debounce infrastructure following the EXACT pattern from LightDetailPage.xaml.cs (lines 357-372):

1. Add private fields near line 50 (with other timer fields):
```csharp
private DispatcherTimer? _brightnessDebounceTimer;
private double _pendingBrightness;
private DispatcherTimer? _colorDebounceTimer;
private (double x, double y) _pendingColor;
```

2. Refactor BrightnessSlider_ValueChanged (currently line 1176):
   - Keep immediate UI updates (BrightnessValueText, ViewModel.SelectedKeyframe.Brightness, RenderTimeline)
   - Move ONLY the API call (UpdateLightForKeyframeAsync) into debounce timer tick
   - Use same 150ms interval as LightDetailPage

3. Refactor KeyframeColorPicker_ColorChanged (currently line 1142):
   - Keep immediate UI updates (color conversion, ViewModel.SelectedKeyframe.Color, RenderTimeline)
   - Move ONLY the API call (UpdateLightForKeyframeAsync) into debounce timer tick
   - Store pending color as (x, y) tuple after conversion (not RGB)
   - Use same 150ms interval

**Pattern to follow:**
```csharp
if (_brightnessDebounceTimer == null)
{
    _brightnessDebounceTimer = new DispatcherTimer
    {
        Interval = TimeSpan.FromMilliseconds(150)
    };
    _brightnessDebounceTimer.Tick += async (s, args) =>
    {
        _brightnessDebounceTimer.Stop();
        // API call here
    };
}
_brightnessDebounceTimer.Stop();
_brightnessDebounceTimer.Start();
```

**Critical:** UI updates (BrightnessValueText.Text, ViewModel.SelectedKeyframe assignment, RenderTimeline()) MUST appear BEFORE the `_debounceTimer.Start()` call. The timer start must be the LAST thing in the handler to ensure responsive feel.
  </action>
  <verify>
1. Build succeeds: `dotnet build src/HueWindows/HueWindows.csproj -p:Platform=x64`
2. Grep confirms debounce timers exist: `grep -n "_brightnessDebounceTimer\|_colorDebounceTimer" src/HueWindows/Views/SceneBuilderPage.xaml.cs`
3. Grep confirms 150ms interval: `grep -n "FromMilliseconds(150)" src/HueWindows/Views/SceneBuilderPage.xaml.cs`
4. Verify UI updates happen before timer start (for brightness handler):
   - `grep -n -A30 "BrightnessSlider_ValueChanged" src/HueWindows/Views/SceneBuilderPage.xaml.cs | grep -E "(BrightnessValueText|RenderTimeline|\.Start\(\))"`
   - Confirm BrightnessValueText and RenderTimeline lines appear BEFORE .Start() line
5. Verify UI updates happen before timer start (for color handler):
   - `grep -n -A30 "KeyframeColorPicker_ColorChanged" src/HueWindows/Views/SceneBuilderPage.xaml.cs | grep -E "(SelectedKeyframe\.Color|RenderTimeline|\.Start\(\))"`
   - Confirm SelectedKeyframe.Color and RenderTimeline lines appear BEFORE .Start() line
6. Manual test: Rapidly drag brightness slider - observe single API call after stopping
7. Manual test: Rapidly adjust color picker - observe single API call after stopping
8. UI text updates immediately during drag (no 150ms delay on visual feedback)
  </verify>
  <done>
Brightness slider and color picker handlers send API updates only after 150ms of inactivity, while UI updates remain immediate (verified by line order in code).
  </done>
</task>

</tasks>

<verification>
1. Build: `dotnet build src/HueWindows/HueWindows.csproj -p:Platform=x64` succeeds
2. Grep confirms debounce timers exist: `grep -n "_brightnessDebounceTimer\|_colorDebounceTimer" src/HueWindows/Views/SceneBuilderPage.xaml.cs`
3. Grep confirms 150ms interval: `grep -n "FromMilliseconds(150)" src/HueWindows/Views/SceneBuilderPage.xaml.cs`
4. Code inspection confirms UI updates (BrightnessValueText, RenderTimeline) occur before timer.Start()
</verification>

<success_criteria>
- [ ] BrightnessSlider_ValueChanged uses DispatcherTimer debounce pattern
- [ ] KeyframeColorPicker_ColorChanged uses DispatcherTimer debounce pattern
- [ ] Both use 150ms interval (matching LightDetailPage)
- [ ] UI updates (text, RenderTimeline) remain immediate (appear before .Start() in code)
- [ ] API calls (UpdateLightForKeyframeAsync) only fire after 150ms inactivity
- [ ] Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-control-refinements/04-01-SUMMARY.md`
</output>
