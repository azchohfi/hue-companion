---
phase: 05-color-picker-overhaul
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-03"]
files_modified:
  - src/HueCompanion/Views/SceneBuilderPage.xaml
  - src/HueCompanion/Views/SceneBuilderPage.xaml.cs
autonomous: false

must_haves:
  truths:
    - "Color picker uses GamutColorPicker instead of WinUI ColorPicker"
    - "Color picker popup can extend beyond timeline area bounds (CPICK-05)"
    - "Selecting color updates gradient bar with matching color (COLOR-01 verified)"
    - "Dragging keyframe preserves color value (COLOR-02 verified)"
    - "Color picker has no RGB input fields (CPICK-03)"
    - "Color picker is smaller than before (CPICK-04)"
  artifacts:
    - path: "src/HueCompanion/Views/SceneBuilderPage.xaml"
      provides: "Timeline with integrated GamutColorPicker"
      contains: "controls:GamutColorPicker"
    - path: "src/HueCompanion/Views/SceneBuilderPage.xaml.cs"
      provides: "Color picker event handling"
      contains: "GamutColorPicker_ColorChanged"
  key_links:
    - from: "SceneBuilderPage.xaml"
      to: "GamutColorPicker"
      via: "control integration"
      pattern: "controls:GamutColorPicker"
    - from: "SceneBuilderPage.xaml.cs"
      to: "keyframe.Color"
      via: "color assignment"
      pattern: "SelectedKeyframe\\.Color"
---

<objective>
Integrate GamutColorPicker into SceneBuilderPage and fix popup positioning.

Purpose: Completes the color picker overhaul by replacing the WinUI ColorPicker with the new GamutColorPicker in the Scene Builder's keyframe editor panel. Also fixes CPICK-05 by moving the SidePanel to allow overflow beyond the timeline area.

Output: Scene Builder uses gamut-aware color picker with proper positioning.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-color-picker-overhaul/05-RESEARCH.md
@.planning/phases/05-color-picker-overhaul/05-01-SUMMARY.md (color conversion consolidation)
@.planning/phases/05-color-picker-overhaul/05-03-SUMMARY.md (GamutColorPicker control)

Current SidePanel structure (SceneBuilderPage.xaml lines 269-357):
- SidePanel is a Border inside Grid.Row="2" (Timeline Area)
- Uses HorizontalAlignment="Right" VerticalAlignment="Bottom"
- Contains ColorPicker with ColorSpectrumShape="Ring"

Fix approach for CPICK-05:
- Move SidePanel to root Grid (outside Row 2)
- Use Canvas positioning for absolute placement
- Allow overflow beyond timeline bounds
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ColorPicker with GamutColorPicker in SidePanel</name>
  <files>src/HueCompanion/Views/SceneBuilderPage.xaml</files>
  <action>
1. Add namespace for controls at top of file:
```xml
xmlns:controls="using:HueCompanion.Controls"
```

2. Find the ColorPicker in SidePanel (around line 303-308):
```xml
<ColorPicker x:Name="KeyframeColorPicker"
             ColorSpectrumShape="Ring"
             IsAlphaEnabled="False"
             IsHexInputVisible="False"
             IsMoreButtonVisible="False"
             ColorChanged="KeyframeColorPicker_ColorChanged"/>
```

3. Replace with GamutColorPicker:
```xml
<controls:GamutColorPicker x:Name="KeyframeColorPicker"
                           ColorChanged="GamutColorPicker_ColorChanged"/>
```

4. Remove the Brightness Slider section below (lines ~311-330) since GamutColorPicker includes its own brightness slider. Find:
```xml
<!-- Brightness Slider -->
<StackPanel Spacing="8">
    ...BrightnessSlider...
</StackPanel>
```
And remove the entire StackPanel.

5. Update the Color label StackPanel (around line 300-302) to use less margin since the picker is now smaller:
```xml
<StackPanel Spacing="4">
    <TextBlock Text="Color"
               Style="{StaticResource BodyStrongTextBlockStyle}"/>
    <controls:GamutColorPicker x:Name="KeyframeColorPicker"
                               ColorChanged="GamutColorPicker_ColorChanged"/>
</StackPanel>
```
  </action>
  <verify>
File contains `controls:GamutColorPicker` and no longer contains `<ColorPicker`.
File contains `xmlns:controls="using:HueCompanion.Controls"`.
  </verify>
  <done>SceneBuilderPage.xaml uses GamutColorPicker instead of WinUI ColorPicker</done>
</task>

<task type="auto">
  <name>Task 2: Update code-behind for GamutColorPicker events</name>
  <files>src/HueCompanion/Views/SceneBuilderPage.xaml.cs</files>
  <action>
1. Find the KeyframeColorPicker_ColorChanged handler (around line 1140-1174):
```csharp
private void KeyframeColorPicker_ColorChanged(ColorPicker sender, ColorChangedEventArgs args)
{
    ...
}
```

2. Replace with new handler for GamutColorPicker:
```csharp
private void GamutColorPicker_ColorChanged(object? sender, HueCompanion.Core.Models.HueColor color)
{
    if (ViewModel.SelectedKeyframe == null || _isUpdatingPanel)
        return;

    // Update keyframe color
    ViewModel.SelectedKeyframe.Color = color;

    // Update brightness from picker
    if (sender is Controls.GamutColorPicker picker)
    {
        ViewModel.SelectedKeyframe.Brightness = (int)(picker.Brightness * 100);
    }

    // Mark scene as modified
    ViewModel.HasUnsavedChanges = true;

    // Invalidate timeline to show new color
    TimelineCanvas?.Invalidate();

    // Live preview on light
    PreviewColorOnLight(color);
}
```

3. Remove the old BrightnessSlider_ValueChanged handler if it exists separately (the logic is now in GamutColorPicker_ColorChanged).

4. Update UpdateSidePanelFromKeyframe() (around line 1100-1135) to set GamutColorPicker color:

Find:
```csharp
KeyframeColorPicker.Color = Windows.UI.Color.FromArgb(255, rgb.R, rgb.G, rgb.B);
```

Replace with:
```csharp
KeyframeColorPicker.SelectedColor = keyframe.Color;
```

Also update brightness slider reference if it exists:
Find:
```csharp
BrightnessSlider.Value = keyframe.Brightness;
```

Replace with (if using the picker's built-in brightness):
```csharp
// Brightness is now part of KeyframeColorPicker
// No separate slider to update
```

5. Add the PreviewColorOnLight helper if not present:
```csharp
private async void PreviewColorOnLight(HueColor color)
{
    if (ViewModel.SelectedTrack?.LightId == null)
        return;

    try
    {
        await _hueBridgeService.SetLightColorAsync(ViewModel.SelectedTrack.LightId, color);
    }
    catch
    {
        // Ignore preview errors
    }
}
```
  </action>
  <verify>
`dotnet build src/HueCompanion/HueCompanion.csproj -p:Platform=x64` succeeds.
Grep for "GamutColorPicker_ColorChanged" returns match.
  </verify>
  <done>Code-behind handles GamutColorPicker events correctly</done>
</task>

<task type="auto">
  <name>Task 3: Fix SidePanel positioning to allow overflow (CPICK-05)</name>
  <files>src/HueCompanion/Views/SceneBuilderPage.xaml</files>
  <action>
Move SidePanel from Grid.Row="2" (timeline area) to root Grid with absolute positioning.

1. Find the SidePanel Border (around line 269):
```xml
<Border x:Name="SidePanel"
        Width="280"
        MaxHeight="500"
        Margin="0,0,16,16"
        HorizontalAlignment="Right"
        VerticalAlignment="Bottom"
        ...>
```

2. Cut the entire SidePanel Border (lines ~269-357)

3. Paste it after the closing `</Grid>` of Row 2 (the Timeline Area Grid), but still inside the root Grid. Add Grid.RowSpan to overlay all rows:

```xml
<!-- Move SidePanel outside Row 2 to allow overflow -->
<Border x:Name="SidePanel"
        Grid.RowSpan="4"
        Width="280"
        MaxHeight="500"
        Margin="0,0,52,16"
        HorizontalAlignment="Right"
        VerticalAlignment="Bottom"
        Visibility="Collapsed"
        Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
        CornerRadius="8"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
        BorderThickness="1">
    ...content...
</Border>
```

Key changes:
- Add `Grid.RowSpan="4"` to span all rows
- Adjust Margin from 16 to 52 to account for page padding (36px) + original margin (16px)
- HorizontalAlignment="Right" + VerticalAlignment="Bottom" positions at bottom-right of entire page

4. Do the same for EventSidePanel (lines ~359-430):
```xml
<Border x:Name="EventSidePanel"
        Grid.RowSpan="4"
        Width="280"
        Margin="0,0,52,16"
        HorizontalAlignment="Right"
        VerticalAlignment="Bottom"
        Visibility="Collapsed"
        ...>
```

This allows both panels to extend above the timeline area if needed.
  </action>
  <verify>
Grep for `Grid.RowSpan="4"` in SceneBuilderPage.xaml returns matches for SidePanel and EventSidePanel.
SidePanel is no longer inside the Grid.Row="2" section.
  </verify>
  <done>SidePanel and EventSidePanel can overflow beyond timeline bounds (CPICK-05 fixed)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete color picker overhaul:
- GamutColorPicker replaces WinUI ColorPicker
- Gamut-aware saturation limits prevent impossible colors
- Smaller picker (~50% of original size)
- No RGB input fields
- SidePanel can extend beyond timeline area
- Color conversions consolidated for visual consistency
  </what-built>
  <how-to-verify>
1. Launch dev build: `.\hue dev`
2. Navigate to Scene Builder (create or load a scene)
3. Click on a keyframe to open the side panel

Verify CPICK-01 (hue ring with adaptive saturation):
- Color picker shows hue ring (outer) and saturation area (inner)
- Some saturation regions appear grayed out (outside gamut)

Verify CPICK-02 (all colors realizable):
- Select various colors on the picker
- Physical Hue light should display a color matching the selection

Verify CPICK-03 (no RGB fields):
- No text input fields for R, G, B values visible

Verify CPICK-04 (smaller size):
- Color picker is noticeably smaller than before (~160px wide)

Verify CPICK-05 (popup overflow):
- Panel appears at bottom-right
- If you had a tall panel, it could extend above the timeline area

Verify COLOR-01 (gradient bar matches picker):
- Select a color on the picker
- Gradient bar at keyframe position shows matching color
- Keyframe circle also shows matching color

Verify COLOR-02 (drag preserves color):
- Note the keyframe's color
- Drag keyframe to a different time position
- Color should be identical after drag
  </how-to-verify>
  <resume-signal>Type "approved" if all requirements pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Build and run:
```
.\hue dev
```

Navigate to Scene Builder and test all requirements:
- CPICK-01: Hue ring with adaptive saturation
- CPICK-02: All colors realizable on bulbs
- CPICK-03: No RGB input fields
- CPICK-04: Smaller picker size
- CPICK-05: Popup can overflow
- COLOR-01: Gradient bar matches picker
- COLOR-02: Drag preserves color
</verification>

<success_criteria>
1. GamutColorPicker integrated into SceneBuilderPage
2. SidePanel positioned with Grid.RowSpan to allow overflow
3. Color picker is smaller than original WinUI ColorPicker
4. No RGB text input fields visible
5. Build succeeds with no errors
6. Human verification confirms all CPICK and COLOR requirements pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-color-picker-overhaul/05-04-SUMMARY.md`
</output>
